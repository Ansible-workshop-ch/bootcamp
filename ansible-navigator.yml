---
ansible-navigator:
  mode: stdout

  execution-environment:
    enabled: true

    pull:
      policy: missing

  playbook-artifact:
    enable: true
    save-as: >-
      artifacts/{playbook_name}-
      {playbook_status}-
      {time_stamp}.json

  logging:
    level: warning